# E2E Test Configuration
# Copy this file to .env and fill in your values.
# NEVER commit .env files to version control.

# Required for live API tests (tests skip gracefully when missing)
OPENROUTER_API_KEY=

# Optional: custom workspace directory for setup script
# E2E_WORKSPACE_DIR=
