## 1. Package & Binary Rename

- [x] 1.1 Rename `packages/cli/package.json`: name `@clawmasons/chapter` → `@clawmasons/mason`, bin field `clawmasons` → `mason`, update description to "Mason — AI agent packaging, governance, and runtime orchestration"
- [x] 1.2 Rename bin file `bin/clawmasons.js` → `bin/mason.js` (or update the bin path reference)
- [x] 1.3 Update Commander program name in `packages/cli/src/cli/index.ts`: `.name("clawmasons")` → `.name("mason")`
- [x] 1.4 Update root `package.json` description: replace "Clawmasons Chapter" with "Mason"

## 2. Config Directory Rename (.clawmasons → .mason)

- [x] 2.1 Update `WORKSPACE_DIRS` array in `packages/cli/src/cli/commands/init.ts`: `.clawmasons` → `.mason`
- [x] 2.2 Update config path in init.ts: `.clawmasons/chapter.json` → `.mason/chapter.json`
- [x] 2.3 Update idempotency check in init.ts: `.clawmasons` → `.mason`
- [x] 2.4 Update `ensureGitignoreEntry` in `packages/cli/src/cli/commands/build.ts`: `.clawmasons` → `.mason`
- [x] 2.5 Update build output paths in build.ts: `.clawmasons/docker/` → `.mason/docker/`
- [x] 2.6 Update `SENTINEL_RELATIVE_PATH` in `packages/cli/src/materializer/docker-generator.ts`: `.clawmasons/empty-file` → `.mason/empty-file`
- [x] 2.7 Update docker build root default in docker-generator.ts: `.clawmasons/docker` → `.mason/docker`
- [x] 2.8 Update session directory paths in docker-generator.ts: `.clawmasons/sessions` → `.mason/sessions`
- [x] 2.9 Update session directory paths in `packages/cli/src/acp/session.ts`: `.clawmasons/sessions` → `.mason/sessions`
- [x] 2.10 Update all comments referencing `.clawmasons/` directory in docker-generator.ts, session.ts, proxy-dependencies.ts

## 3. CLI Command & Message Updates

- [x] 3.1 Update all user-facing log messages, help text, and error messages in `packages/cli/src/cli/commands/*.ts` that reference `clawmasons` → `mason`
- [x] 3.2 Update log prefix in `packages/cli/src/acp/warnings.ts`: `[clawmasons ...]` → `[mason ...]`
- [x] 3.3 Update "Generated by" comments in `packages/cli/src/generator/agent-dockerfile.ts`
- [x] 3.4 Update "Generated by" comments in `packages/cli/src/generator/credential-service-dockerfile.ts`
- [x] 3.5 Update proxy Dockerfile entrypoint in `packages/cli/src/generator/proxy-dockerfile.ts`: `clawmasons` → `mason`
- [x] 3.6 Update `@clawmasons/mcp-agent` comment in `packages/cli/src/generator/agent-dockerfile.ts` (keep package name, update binary/command references only)

## 4. Proxy Dependencies & Package References

- [x] 4.1 Update idempotency check in `packages/cli/src/materializer/proxy-dependencies.ts`: `@clawmasons/chapter` → `@clawmasons/mason`
- [x] 4.2 Update error message in proxy-dependencies.ts: `@clawmasons/chapter` → `@clawmasons/mason`
- [x] 4.3 Update JSDoc comments in proxy-dependencies.ts: `.clawmasons/docker/` → `.mason/docker/`

## 5. Docker Install Pipeline Updates

- [x] 5.1 Update `generateProxyDockerfile()` to copy `mason/` prefix paths instead of `chapter/` (package.json, dist, bin)
- [x] 5.2 Update entrypoint: `["node", "/app/chapter/bin/chapter.js"]` → `["node", "/app/mason/bin/mason.js"]`
- [x] 5.3 Update `runInstall()` to copy artifacts into `proxy/mason/` instead of `proxy/chapter/`
- [x] 5.4 Update `getChapterProjectRoot()` → `getMasonProjectRoot()` (or equivalent function rename)

## 6. Source Code Variable & Constant Renames

- [x] 6.1 Rename `CLAWMASONS_BIN` → `MASON_BIN` in `e2e/tests/helpers.ts`
- [x] 6.2 Rename `clawmasonsDir` variables → `masonDir` across source files
- [x] 6.3 Rename `chapterExec` → `masonExec` and `chapterExecJson` → `masonExecJson` in e2e helpers
- [x] 6.4 Rename any `Clawmasons` or `CLAWMASONS` prefixed identifiers in shared package

## 7. Unit Test Updates

- [x] 7.1 Update assertions in `packages/cli/tests/cli/cli.test.ts`: program name `clawmasons` → `mason`
- [x] 7.2 Update assertions in `packages/cli/tests/cli/build.test.ts`: log output and paths
- [x] 7.3 Update assertions in `packages/cli/tests/cli/init.test.ts`: `.clawmasons` → `.mason`
- [x] 7.4 Update assertions in `packages/cli/tests/cli/init-role.test.ts`
- [x] 7.5 Update assertions in `packages/cli/tests/cli/docker-init.test.ts`
- [x] 7.6 Update assertions in `packages/cli/tests/cli/run-acp-agent.test.ts`: log prefixes
- [x] 7.7 Update assertions in `packages/cli/tests/acp/warnings.test.ts`
- [x] 7.8 Update assertions in `packages/cli/tests/generator/agent-dockerfile.test.ts`
- [x] 7.9 Update assertions in `packages/cli/tests/generator/credential-service-dockerfile.test.ts`
- [x] 7.10 Update assertions in `packages/cli/tests/materializer/*.test.ts`: `.clawmasons` → `.mason` paths
- [x] 7.11 Update assertions in `packages/cli/tests/cli/mason-init-repo.test.ts`
- [x] 7.12 Update any remaining test files referencing `clawmasons` binary or `.clawmasons` paths

## 8. E2E Test Updates

- [x] 8.1 Update `e2e/tests/helpers.ts`: binary path, helper function names
- [x] 8.2 Update `e2e/tests/build-pi-runtime.test.ts`: command and assertion references
- [x] 8.3 Update `e2e/tests/build-pipeline.test.ts`: binary name, symlink assertions, package references
- [x] 8.4 Update `e2e/tests/docker-proxy.test.ts`: command references
- [x] 8.5 Update any other e2e test files referencing `clawmasons`

## 9. Shared Package Updates

- [x] 9.1 Update `packages/shared/src/mason/proposer.ts` if it references `clawmasons` or `.clawmasons`
- [x] 9.2 Update shared package tests referencing `.clawmasons`
- [x] 9.3 Update `packages/shared/package.json` if name needs changing

## 10. Documentation & Config Updates

- [x] 10.1 Update `README.md`: all `clawmasons` → `mason` binary references, update description
- [x] 10.2 Update `DEVELOPMENT.md`: all `clawmasons` → `mason` references
- [x] 10.3 Update `docs/get-started.md`: CLI command references
- [x] 10.4 Update `docs/security.md`: references
- [x] 10.5 Update `.gitignore`: `.clawmasons` → `.mason`
- [x] 10.6 Update `vitest.config.ts`: any `clawmasons` references
- [x] 10.7 Update `skills/mason/SKILL.md` and `skills/mason/templates/role-template.md`: `.clawmasons` → `.mason`

## 11. Spec Files Updates

- [x] 11.1 Update all `openspec/specs/*/spec.md` files that reference `clawmasons` binary or `.clawmasons` paths to use `mason` / `.mason`
- [x] 11.2 Update PRD files in `openspec/prds/` that reference `clawmasons`

## 12. Verification

- [x] 12.1 Run `npx tsc --noEmit` — typecheck passes
- [x] 12.2 Run `npx eslint packages/*/src/ packages/*/tests/` — 12 pre-existing lint errors (unused vars), none introduced by rename
- [x] 12.3 Run `npx vitest run` — all 1140 unit tests pass
- [x] 12.4 Run `cd e2e && npx vitest run --config vitest.config.ts` — 63/73 pass, 10 failures all pre-existing on main (Docker/ACP flakes)
- [x] 12.5 Grep for remaining `clawmasons` references (excluding `@clawmasons/` scope, git history, and node_modules) — none found
