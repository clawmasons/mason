## 1. Generator Types

- [x] 1.1 Create `src/generator/types.ts` with `ToolFilter`, `McpServerEntry`, and `ProxyConfig` types
- [x] 1.2 Create `src/generator/index.ts` re-exporting types and functions

## 2. toolFilter Computation

- [x] 2.1 Create `src/generator/toolfilter.ts` with `computeToolFilters(agent: ResolvedAgent): Map<string, ToolFilter>`
- [x] 2.2 Implement per-app union of all role allow-lists
- [x] 2.3 Implement forge package short name extraction (strip scope and type prefix)

## 3. Proxy Config Generation

- [x] 3.1 Create `src/generator/proxy-config.ts` with `generateProxyConfig(agent: ResolvedAgent): ProxyConfig`
- [x] 3.2 Implement mcpProxy section generation (addr, type, name, version, authTokens, logging)
- [x] 3.3 Implement mcpServers section generation for stdio apps (command, args, env, toolFilter)
- [x] 3.4 Implement mcpServers section generation for remote apps (url, env, toolFilter)
- [x] 3.5 Implement `${FORGE_PROXY_TOKEN}` placeholder in authTokens (actual token generated by `forge install`)

## 4. Public API

- [x] 4.1 Update `src/index.ts` to export generator types and functions

## 5. Tests

- [x] 5.1 Create `tests/generator/toolfilter.test.ts` with unit tests for toolFilter computation
- [x] 5.2 Test single-role agent produces correct per-app toolFilter
- [x] 5.3 Test multi-role agent produces union of allow-lists per app
- [x] 5.4 Test app short name extraction (scoped, unscoped, with/without type prefixes)
- [x] 5.5 Test tools not in any role's allow-list are excluded
- [x] 5.6 Create `tests/generator/proxy-config.test.ts` with unit tests for proxy config generation
- [x] 5.7 Test generated config matches PRD §6.3 structure for the repo-ops example
- [x] 5.8 Test stdio app entry has command, args, env, and toolFilter
- [x] 5.9 Test remote (sse) app entry has url and toolFilter
- [x] 5.10 Test auth token placeholder `${FORGE_PROXY_TOKEN}` is present in config
- [x] 5.11 Test environment variable interpolation (`${VAR}`) is preserved in output
- [x] 5.12 Test mcpProxy section has correct defaults (addr, type, logging)

## 6. Verification

- [x] 6.1 Run `npm test` and confirm all 153 tests pass (125 existing + 28 new)
- [x] 6.2 Run `npm run build` and confirm TypeScript compiles without errors
- [x] 6.3 Run `npm run lint` and confirm no lint errors
